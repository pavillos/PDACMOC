% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/background.R
\name{background}
\alias{background}
\alias{estimate.minutes}
\alias{background.submit}
\alias{background.status}
\alias{background.result}
\alias{background.error}
\alias{background.cancel}
\alias{background.cleanup}
\alias{background.worker}
\title{Background classification}
\usage{
estimate.minutes(n_samples, batch, stroma)

background.submit(expmat, args)

background.status(job)

background.result(job)

background.error(job)

background.cancel(job)

background.cleanup(job)

background.worker(job_dir)
}
\arguments{
\item{n_samples}{Number of samples}

\item{batch, stroma}{Options of the classification}

\item{expmat}{Expression matrix as read by \code{read.counts}}

\item{args}{Named list of arguments for \code{omni.classify} (without expmat)}

\item{job}{Job returned by \code{background.submit}}

\item{job_dir}{Folder of the job}
}
\description{
Run \code{omni.classify} in a separate R process so that the Shiny app stays
responsive and a classification can be cancelled. Each classification ("job") lives
in its own temporary folder: the input, a progress file written by
\code{report.progress}, the package messages, and the result or the error. At most
\code{getOption('PDACMOC.max_jobs', 2)} jobs run at the same time in one app; later
ones wait for a free slot.
}
\section{Functions}{
\itemize{
\item \code{estimate.minutes()}: Rough running time in minutes, from timings measured on the
server (fixed start-up plus a cost per sample, larger with stroma)

\item \code{background.submit()}: Prepare a job; it starts as soon as a slot is free

\item \code{background.status()}: State of a job; starts it when a slot is free

\item \code{background.result()}: Result of a finished job

\item \code{background.error()}: Error message of a failed job

\item \code{background.cancel()}: Stop a job and all its processes

\item \code{background.cleanup()}: Forget a job and delete its folder

\item \code{background.worker()}: Entry point of the worker process

}}
\author{
Villoslada-Blanco, Pablo
}
